{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Shared secret for booking-code generation. MUST be identical to the value
  // in SlicerBookingHelper/appsettings.json or offline codes won't match.
  // Change this to your own random string before deploying.
  "BookingCodeSecret": "change-me-to-a-long-random-secret-string"
}
